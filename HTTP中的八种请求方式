（1）OPTIONS 返回服务器针特定资源所支持的HTTP请求方法。也可以利用向Web服务器发送'*'的请求来测试服务器的功能性。
（2）HEAD 向服务器索要与GET请求相一致的响应，只不过响应体不被返回。这一方法可以在不必传输整个响应内容的情况下，就可以获取包含在响应消息头中的元信息。　
（3）GET 是一种向服务器索要数据的一种请求，由于不同浏览器和服务器对url的长度有不同的要求，因此在使用GET请求时会受到url长度的限制，并且因为GET请求中的参数是直接拼接在url中，会出现在浏览器地址栏中，因此安全性不是很好，在分页时一般会用GET请求。　
（4）POST 是一种向服务器提交数据的请求（例如提交表单或者上传文件）。POST请求数据被包含在请求体中，由于不是url传值，理论上大小不会受到限制，但一般浏览器或服务器会对POST请求数据的大小进行限制。因为POST请求对参数进行了隐藏，因此相对于GET方式来讲，安全性要好一些，在做数据查询时，建议使用GET方式，在对数据进行添加、修改等操作时，建议使用POST方式，POST请求可能会导致新的资源的建立和/或已有资源的修改。　
（5）PUT 向指定资源位置上传其最新内容。　
（6）DELETE 请求服务器删除Request-URI所标识的资源。　
（7）TRACE 回显服务器收到的请求，主要用于测试或诊断，　
（8）CONNECT HTTP/1.1协议中预留给能够将连接改为管道方式的代理服务器。
